#include "../include/Transition.hpp"

/**
 * @file Transition.cpp
 * @brief Implementación mínima de Transition
 *
 * Contiene el constructor por defecto. La clase Transition representa una
 * regla de transición del autómata de pila, incluyendo estado origen,
 * símbolo de entrada (o epsilon), símbolo a desapilar, estado destino y
 * símbolos a apilar.
 */

/**
 * @brief Constructor por defecto de Transition
 *
 * Inicializa una transición vacía. La lógica y los constructores más
 * completos se definen en la cabecera y se usan desde el parser.
 */
Transition::Transition() {}
